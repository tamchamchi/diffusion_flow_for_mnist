# Copy this file to .env and adjust for your machine. .env itself is
# gitignored (may contain machine-specific absolute paths); this template
# is the only one tracked in git.
#
# Root directory containing all checkpoint subdirectories referenced by the
# scripts/*.sh scripts: ckpt_ddpm/, ckpt_score/, ckpt_score_flow/,
# ckpt_flow/, ckpt_flow_diff/, ...
CKPT_ROOT=/mnt/mmlab2024nas/anhndt/flow_diffusion_for_mnist
